SHELL=/bin/bash
PATH=/sbin:/bin:/usr/sbin:/usr/bin:/usr/local/bin
MAILTO=root
HOME=/
# Nightly reboots
#15 0 * * * root /usr/local/bin/reboot_to_win 
#45 6 * * * root /usr/local/bin/random_machine_reboot
# GRUB conf updater
#01 * * * * root /usr/local/bin/grub2
# Stagger puppet agent
10,40 * * * * root sleep $((RANDOM \% 1500)) && puppet agent -t > /dev/null 2>&1
