###############################################################################
# << FILE MANAGED BY PUPPET >>
# Manual changes are likely to be overwritten
###############################################################################
SEMESTER="<%= scope.lookupvar('semester') %>"
BRANCH="<%= scope.lookupvar('branch') %>"
BUILD="<%= scope.lookupvar('build') %>"
TEMPLATE="<%= scope.lookupvar('release') %>"
TORRENT="<%= scope.lookupvar('torrent') %>"
PRESEED="<%= scope.lookupvar('preseed') %>"
SYNCPRE="<%= scope.lookupvar('syncpre') %>"
